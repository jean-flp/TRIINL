import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    custom: {
      white: "#FFFFFF", // Branco
      butterscotch: "#FCEACA", // Butterscotch
      strawberryMilkshake: "#FBD9CD", // Milkshake de morango
      mintGlow: "#D4DCD1", // Menta
      goldenGoose: "#F1BC52", // Ganso dourado
      brownMoss: "#543618", // Musgo Marrom
    },
  },
});

export default theme;
